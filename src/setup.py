"""Create the spotify_adaptors package."""

from setuptools import setup

setup(name='spotify adaptors',
      version='0.1',
      description='Adaptors for Spotify dictionaries.',
      author='Joshua Litven',
      packages=['spotify_adaptors'])
